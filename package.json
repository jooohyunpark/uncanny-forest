{
  "name": "uncanny-forest",
  "version": "1.0.0",
  "description": "",
  "source": "index.html",
  "scripts": {
    "start": "parcel serve index.html --no-cache --open",
    "build": "npm run clean && parcel build index.html --no-source-maps --no-cache",
    "preview": "npm run build && http-server ./dist",
    "clean": "rm -rf dist/*"
  },
  "keywords": [],
  "author": "Joohyun Park",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "jquery": "^3.7.1"
  }
}
